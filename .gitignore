# secrets & runtime state — never publish
config.json
*.env
secrets.json
.kaisen_snapshots/
*.skill_stats.json
.env
kai_runs.json
engine_pool.json
campaign.json
campaign_bugs.jsonl


# generated projects are runtime data, not framework code — never publish
projects/*
!projects/.gitkeep

# legacy / old harness data (kept locally, not published)
legacy/

# python
__pycache__/
*.pyc
*.pyo
*.so
*.o

# user data
notes.json
debug_logs/
*.log

# test caches
.pytest_cache/
.ruff_cache/

# benchmark data (huge)
temp/
