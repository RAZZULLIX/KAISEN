EDIT SCOPE — you may ONLY modify the function(s): {{scope_names}}
Do not change any other function, and do not add new top-level functions or
global state outside those names. The harness verifies a diff footprint; a
change outside the allowed scope is REJECTED regardless of how fast it is.
