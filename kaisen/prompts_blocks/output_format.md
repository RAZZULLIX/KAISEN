OUTPUT FORMAT — MANDATORY:
Reply with ONLY the complete improved program inside a single code block.
- One code block, tagged ```{{lang_fence}}
- No explanations, no bullet lists, no "here is the code", no diff markers.
- The code must be the FULL program (not a fragment, not a patch) satisfying
  the contract above.
- If you cannot improve it, output the best complete program you can —
  an invalid output is worse than an unchanged one.
