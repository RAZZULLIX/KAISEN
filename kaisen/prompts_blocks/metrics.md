SCORING — the harness parses the metrics below from the program's output
and combines them with the declared weights into a composite fitness
(higher is always better after normalization). Optimize the weighted
composite, not any single metric.

{{metric_table}}

{{best_metrics}}
